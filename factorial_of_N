def factorial(N):
    fact=1
    for i in range(1, N+1):
        fact*=i
    return fact

N=int(input("Enter a natural number: "))
result=factorial(N)
print(f"Factorial of {N} is : {result}")
